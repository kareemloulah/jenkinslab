pipeline {
    agent any
    stages {
        stage('getcode') {
            steps {
                checkout scm
            }
        }
        stage('build') {
            steps {
                dir(jenkinsTask) {
                    script {
                    def appimage = docker.build "loulah/nodeapp:${env.BUILD_NUMBER}"
                    }
                }
            }
        }
        stage('deploy') {
            steps {
                script {
                    appimage.run()
                }
            }
        }
    }
}